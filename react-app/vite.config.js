import { defineConfig } from 'vite'

export default defineConfig({
  // Use relative base path so it works seamlessly on GitHub Pages
  // regardless of what you name your repository
  base: './',
})
